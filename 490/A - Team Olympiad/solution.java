import java.util.*; public class Team_Olympiad {    public static void main(String[] args) {        Scanner sc=new Scanner(System.in);        int n = sc.nextInt();         HashMap<Integer,List<Integer>> map = new HashMap<>();         for(int i=0;i<n;i++){            int x = sc.nextInt();            if (!map.containsKey(x)) map.put(x,new ArrayList<>());             map.get(x).add(i);        }         if (map.size() < 3){            System.out.println(0);        }        else {            int minsize = Math.min(map.get(1).size(), Math.min(map.get(2).size(), map.get(3).size()));            System.out.println(minsize);            if (minsize > 0) {                int i = 0;                while (i < map.get(1).size() && i < map.get(2).size() && i < map.get(3).size()) {                    int a = map.get(1).get(i);                    int b = map.get(2).get(i);                    int c = map.get(3).get(i);                    System.out.println((a+1) + " " + (b+1) + " " + (c+1));                     i++;                }            }        }    }}